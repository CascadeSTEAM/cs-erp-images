# Session Note: 2026-07-24 — Builder site + CS infra

**Date:** 2026-07-24
**Author:** NetYeti @ phoenix
**Focus:** Builder site + CS infra

## Summary

Rebuilt the builder-site-deploy skill into a pipeline (crawl -> analyze -> compose -> componentize -> publish) and deployed the Cascade STEAM website to new.cascadesteam.org as token/component/template-based Builder pages. Split ERP hostnames: erp.cascadesteam.org serves login/desk via a Caddy upstream_host rewrite (OpsKit PR #60), support.cascadesteam.org redirects to Helpdesk. Replaced stale Frappe scheduled backup with native backups (backup_limit=14, --with-files) plus an off-box cspve2 pull, after dropping an overbuilt borg design. Established the OpsKit-subagent infra policy in AGENTS.md/CLAUDE.md (PR #5) and memory; all live-infra work ran via OpsKit subagents and the operational record was pushed to the private env-cs repo.

## Decisions Made

- All infra/deploy/maintenance changes route through an OpsKit subagent (AGENTS.md/CLAUDE.md + memory)
- new.cascadesteam.org is a separate Builder site (future cascadesteam.org), not merged into support
- erp./support. hostname split via Caddy Host-rewrite + root redirect, not Frappe site-level home_page
- Backups: native Frappe scheduled + off-box cspve2 pull; borg dropped as overbuilt for the ~165MB dataset
- builder-site-deploy: scrape a client's live site into a full Builder replica; a template is mandatory per site

## Active Plans

_No active (in-progress/approved) plans._

## Commits This Session

```
ad576e9 docs: add website backup & export guide (#6)
4f3cd24 docs: add website backup & export guide
f9fcbcf docs: add AGENTS.md + CLAUDE.md — route infra/deploy through OpsKit subagents (#5)
53cf5a5 docs: add AGENTS.md + CLAUDE.md — route infra/deploy through OpsKit subagents
fed0784 feat: per-image branch overrides, cs image refresh
f61db28 Merge pull request #4 from CascadeSTEAM/feat/grouchys-use-case
7ac2e6a docs: fix stale local-build note — Containerfile is vendored, not fetched
e81650f feat: add grouchys use case
```

## Uncommitted Changes (at session end)

```
?? proposals/
```

## Next Session Should Start With

- Reconcile environments/cs inventory.yml refactor, then commit the entangled caddy/cloudflare IaC cleanly
- Decide fate of proposals/builder-site-deploy-skill.md (commit historical or delete)
- Optional: codify the CT111 frappe-backup + cspve2 off-box timers into an OpsKit playbook
- When Bitwarden is unlocked, store pending items; fix CT111 broken system-python cryptography
